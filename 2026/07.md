## 7/1
SQL
'insert into テーブル名 (カラム名1, カラム名2, ...) values (値1, 値2, ...);'
'update テーブル名 set カラム名1=値1, カラム名2=値2, ... where 条件;'
'FROM テーブルA
JOIN テーブルB ON テーブルA.カラム = テーブルB.カラム'

## 7/2
DISTINCT 重複したレコードを除外
継承 = 親の機能を丸ごと引き継ぐこと
オーバーライド = 引き継いだメソッドを、子クラス側で上書き(再定義)すること
publicメソッド　クラスの外部からでも自由に呼び出せるメソッド
privateメソッド　クラスの外側からは呼び出せないメソッド

## 7/3
リファクタリングで見る箇所
同じコードの重複
長すぎる関数・クラス
深いネスト
意味の分かりにくい変数名
引数が多すぎる関数
密結合なモジュール

## 7/4
Railsのconcernsフォルダは、モデルやコントローラーに共通するロジックをモジュールとして切り出し、再利用するための仕組み

vimtutor vimでの操作を練習した

## 7/5
パンカビアプリ不具合修正
通知作成のフィルタが間違っていた
内容に重複があった

## 7/6
Vim操作難しくてイライラする
SQLの本買った

## 7/8
/〇〇　で　〇〇検索
dw 単語ごと削除
vim . でファイルを一覧表示

## 7/9
- ゼロから始めるデータベース操作
Homebrew ポスグレ18 インストール

## 7/10
NULLを含んだ演算子は全部NULLになる

## 7/11
selectで条件に合う要素だけを新しい配列として取り出す
C# .cspojはプロジェクト設定ファイル
MAINメソッドで実行開始地点を指定する必要がある

## 7/12
HTTPは一回ごとに通信が切れるステートレスなプロトコル
ログインなどの情報はセッションIDによって管理している

## 7/13
- Ruby: select = 条件に合うものを残す（絞り込み）
- Ruby: map = 各要素を変換する
- C#: Where = 絞り込み
- C#: Select = 変換

## 7/14
- SPI実施

## 7/15
HAVINGつかえるのはGROUPで絞った時のみ
全角空白はエラーになるので気を付ける

## 7/17
- COBOLは企業の事務処理や基幹システムのために作られたプログラミング言語。1959年に誕生し、60年以上経った今でも銀行、保険会社、官公庁、大企業のシステムで現役で使われている。
- 「開発の流れ」を解説した入門書を読みたい。

## 7/18
- トランザクション中にエラーが出てロールバックすると最初からになる

## 7/19
Kaggle

## 7/20
| 言語        | よく使われるAPサーバー                   |
| --------- | ------------------------------ |
| Ruby      | Puma                           |
| Java      | Tomcat                         |
| Python    | Gunicorn、uWSGI                 |
| Node.js   | Node.jsランタイム（Expressなどがその上で動作） |
| C# (.NET) | Kestrel                        |

SQLの実行順序
FROM→WHERE→GROUP BY →SELECT→ORDER BY

## 7/21
自分が攻撃者になったつもりで擬似的に攻撃を行う
ペネストレーションテスト

## 7/23
NaN  Not a Number

## 7/24
Vim練習

## 7/26
ABS 絶対値
MOD 剰余　moduloの略
MOD(7,3) = 1 ※SQL Serverでは％を使う
LOWER 小文字化

## 7/27
COALESCE NULLを値に変換する

## 7/28
UNION 和
UNION ALL 重複行を削除しない
INTERSECT 交差 テーブルの共通部分を選択
EXCEPT 差

## 7/29
prototype JSのオブジェクト機能を継承するための仕組み

## 7/30
.NET Framework C#を動かすための土台
現在は.NET
ASP.NET Coreはフレームワーク
JavaはJVM

## 7/31
外部結合は元のテーブルにない外部から情報を結果に持ってくる
