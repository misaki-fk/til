## 3/1
- データはパケットという値で送られる
- シーケンス番号とACK番号でパケットが受信側に届いたことを確認する
## 3/2
request.refererで前画面に戻るボタンを作ったが、
構造が２層以上になっていると戻るの戻るができなくてループした
back_pathという変数を使ってURLをわ刺すように変更した
## 3/3
- LINEで共有機能の実装
- LINEエンドポイントの違いを理解
- Webとスマホで挙動が違う
- text / url パラメータの挙動差を検証
- モバイル判定で分岐実装
- helper内にロジック整理
## 3/4
| 種類          | 役割         |
| ----------- | ---------- |
| **MACアドレス** | 機器そのもののID  |
| **IPアドレス**  | ネットワーク上の住所 |
MAC = Media Access Control　アップルのマックでは無い
MACアドレスはイーサネットで機器を識別するための番号

IPアドレスの確認
```
ipconfig getifaddr en0
```

## 3/5
- 本番環境ようのログインメール送信の設定
  1 Gmailアプリパスワード作る
  2 Render環境変数設定
  3 production.rb設定
なぜか送信されずエラー
続きは明日

## 3/6
- gmailのサーバーはRenderでブロックされている？
　Renderを含む多くのホスティングサービスでは、スパム対策のため ポート25、587、465などのSMTPポートが制限 されていることが多い
- SendGrid
  無料で1日100通まで送れる
  Renderとの相性良いはず

## 3/7
- Resend APIを利用したパスワードリセットメール送信に変更
  NoMethodError (undefined method edit_password_url' for #

## 3/8
ユーザー
↓
Devise
↓
DeviseMailer
↓
Resend API
↓
メール送信
## 3/9
アプリ微修正

## 3/10
ログアウト導線実装
ゲストIDがクッキーに残って、通常ログインボタンを押しても時にゲストログインしてしまう
本リリまでにやること洗い出し

## 3/11
- gap-2 は Tailwind CSS のクラスで、要素同士の間に余白（間隔）を作るという意味
- OGP画像の生成
- モックアップ

## 3/12
- オートネゴシエーション
  オートネゴシエーションとは、接続する相手によって自分の通信速度や通信モードを切り替えることが出来る機能

## 3/13
ルーターは中継部分とポート部分で構成される
経路表から中継先を判断してパケットを中継している
ポート部分でパケットを受信する

本リリースまでの追加機能のissue見積もり

## 3/14
本リリースまでのすることissue作成

## ３/15
- アプリ制作
DBカラムの追加
数量の補正値を追加
- 自己分析

## 3/16
```
member do
  patch :increase
end
```
memberとは
id付きのものに対する処理
member routeはブロックでかく

## 3/18
Deviseのrememberableでログイン状態を保持できるように実装した
記述記事を書いた
https://qiita.com/fkmiso/items/652529b53ca5ba08ed6e

## 3/19
- references 外部キー
- whenever = cronをRailsっぽく書けるgem
- cronとは、決まった時間に自動で処理を実行する仕組み（OSの機能）
- Cron Job Renderの定期処理

## 3/20
独自ドメインの取得
- DNS理解
- Cloudflare
- SSL

## 3/21
Googleアナリティクス設定

## 3/22
- Turboによる必要な部分だけの更新
- Google Cloud Vision API

## 3/22
画像
 ↓
OCR（文字取得）
 ↓
テキスト解析
 ↓
消費期限っぽい日付を抽出
 ↓
Date型で返す

## 3/23
curl HTTPをブラウザの代わりに送るツール
フロントなしでテストできる
OCRで自動入力
  - PCで画像をアップロードして入力　OK
  - スマホでカメラを起動し読み取り　まだ
