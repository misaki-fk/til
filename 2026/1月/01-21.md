- 本番環境DB接続確認
本番ではなく開発ようになってしまう
→RenderのDocker Command（Start Command）の設定が必要だった
→bin/rails db:migrate && bin/rails server -b 0.0.0.0
bがダメな模様　デプロイエラー
bin/rails db:migrate && bin/rails server　&&で引っかかりデプロイエラー
無料プランだから？

