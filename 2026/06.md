## 6/1
- Bcrypt　ビークリプトはパスワードを安全にハッシュ化するアルゴリズム

## 6/2
- atcoderでの力だめしも取り入れていきたい
- LINE APIの実装についても整理したい

## 6/3
- 企業研究
- 自分の強み弱みについてまとめを行った

## 6/4
- LINE通知機能の実装部分の復習

## 6/5
- 長所短所　これまでの経験
- エンジニアになりたいと思った理由
- 何をがんばったか

## 6/6
- 入力のデータ変換。整数なら gets.to_i、文字列なら gets.chomp

## 6/7
- 求人調査

## 6/8
一つずつ取り出す
```
(1..i).each do |i|
  puts i
end
```
回数分取り出す
```
5.times do |i|
  puts i
end
```
条件が真の間繰り返す
```
i = 1

while i <= 5
  puts i
  i += 1
end
```
値を変換して新しい配列を作る
```
numbers = [1, 2, 3]

result = numbers.map do |n|
  n * 2
end

p result
```

## 6/9
- WordPressはPHP

## 6/10
["1", "2", "3"].map { |x| x.to_i }

出力結果
[1,2,3]

gets.split.map(&:to_i)
空白区切りの整数を配列で取得

## 6/11
- 企業軸は何かを考えた

## 6/12
```
a = [1, 3, 4, 6, 8]

# 正方向
#  0  1  2  3  4

# 逆方向
# -5 -4 -3 -2 -1
```

## 6/13
- SSL/TSLは、インターネット上でやり取りするデータを暗号化して送受信する仕組み
- httpsはSSL／TLSで保護されている

## 6/14
- abs Absolute Value 絶対値

## 6/15
- EC2はAWS上で自由に使える仮想サーバー
- AWSなどのクラウド基盤を利用することで、自前でサーバーを持たなくてもWebサービスやSaaSを運用できる

## 6/16
- RAG 検索拡張生成
  データを追加できる。与えられた情報源に基づいて回答するため貼るしネーションを防げる

## 6/17
gets.to_iをどこにも代入しなければ、入力を読むだけでその値は使わない

## 6/18
- CRUD以外の独自アクションを特定のレコードに追加する時はmemberを使ってルーティングに設定する
- バリデーション(検証)もActiveRecordの機能の一つである
- destroy は1件のモデルインスタンスに対して呼ぶもので、複数件の集合を削除するなら destroy_all を使う
- ぼっち演算子 &. はnilかもしれないオブジェクトに対して、安全にメソッドを呼びたい時に使う

## 6/19
- 三項演算子は短くかけるが、複雑なものに使うと読みにくくなる事があるので注意

## 6/20
- エンドレスメソッド定義
  def greet = 'Hello!'
- garbage collection  使用されなくなったオブジェクトを回収し自動的にメモリを解放する

## 6/21
### SQL
SELECT 列名 FROM テーブル名
SELECT count(*) FROM employees WHERE gender = 'F';

COUNT(): 行数をカウント
AVG(): 平均値を計算
MAX(): 最大値を取得
MIN(): 最小値を取得
---------
to_f：整数 → 小数に変換（3 → 3.0）
floor：小数 → 整数に変換（3.7 → 3）

## 6/22
&:メソッド
['ruby','java','python'].map{ |s| s.upcase }
&:で書き換え
['ruby','java','python'].map(&:upcase)

## 6/23
- scan は文字列の中から、正規表現にマッチする部分を全部探して配列で返すメソッド
- concat 配列同士を連結するメソッド

## 6/24
- splat演算子 *
  多重代入で残りの全要素を配列として受け取る、または無視する


## 6/25
- DOM
  ブラウザがHTMLを読み込んだ時に、内部で作るツリー構造のこと
  JSはDOMを操作することで表示を変えることができる

  Railsアプリ
├── React / Vue を使う場合  → JS多め、APIでJSON返す
└── Hotwire を使う場合     → JS少なめ、HTMLをそのまま返す